eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:19901/eureka/

server:
  port: 19906
spring:
  application:
    name: service-zuul
zuul:
  routes:
    api-a:
      path: /api-a/**
      serviceId: service-ribbon
    api-b:
      path: /api-b/**
      serviceId: service-feign